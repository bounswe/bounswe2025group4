{
  "name": "bounswe2025group4-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.9.0",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1"
  },
  "lint-staged": {
    "apps/frontend/src/**/*.{js,jsx,ts,tsx}": [
      "pnpm --filter frontend lint --fix",
      "pnpm --filter frontend type-check",
      "pnpm --filter frontend test:unit --related"
    ]
  }
}