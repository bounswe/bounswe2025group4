import { createTheme } from '@mui/material/styles';
import '@mui/x-data-grid/themeAugmentation';

// Define your dark theme options
export const darkTheme = createTheme({
  palette: {
    mode: 'dark',
  },
});
