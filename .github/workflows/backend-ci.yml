name: Jobboard Backend CI

on:
  push:
    paths:
      - "apps/jobboard/backend/**"
  pull_request:
    paths:
      - "apps/jobboard/backend/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Jobboard backend workflow triggered"
