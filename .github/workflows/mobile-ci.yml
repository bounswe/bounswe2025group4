name: Jobboard Mobile CI

on:
  push:
    paths:
      - "apps/jobboard/mobile/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Jobboard mobile workflow triggered"
